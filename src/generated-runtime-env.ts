// Gerado automaticamente no build da Amplify (ver amplify.yml) a partir das
// environment variables configuradas no console, que nao chegam sozinhas no
// runtime do Lambda. NUNCA edite manualmente nem faca commit de valores reais
// aqui - este arquivo fica assim (vazio) no repositorio; o build da Amplify
// sobrescreve com os valores reais antes de compilar, e o resultado nunca
// volta pro git.
export const RUNTIME_ENV: Record<string, string> = {};
